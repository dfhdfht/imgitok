apiVersion: v1
kind: Service
metadata:
  name: dkforeinok
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dkforeinok"
    port: 8080
  selector:
    app: ray
